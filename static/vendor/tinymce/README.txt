TinyMCE 7.9.3 — self-hosted community build, GPL-2.0-or-later.
Vendored (minified subset only) so the editor needs no CDN and no API key.
Re-vendor with: npm install tinymce@7 and copy the same paths.
