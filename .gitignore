# Environment & secrets
.env

# Editor image uploads — user content, not source. Keep the folder, not the files.
static/uploads/*
!static/uploads/.gitkeep

# JSON data store + session key (logins, leads, blog posts live here).
# Never committed: it is live customer data, and a deploy must not overwrite it.
instance/
*.db
*.sqlite3

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# Logs
*.log

# Screenshots (from _shot.py)
*.png
# ...but site imagery is part of the build and must be committed.
!static/img/*.png
!static/img/**/*.png

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
